setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
  config:
    PYTHONPATH: /app

build:
  docker:
    web: Dockerfile
  config:
    DOCKER_BUILDKIT: 1

run:
  web: uvicorn backend.main:app --host 0.0.0.0 --port $PORT --log-level debug